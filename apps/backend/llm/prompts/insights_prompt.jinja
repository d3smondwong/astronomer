Study the complete natal BaZi chart below. The specific section to write is described after it.

Here is the person's complete natal BaZi chart as JSON:

{{ bazi_json }}

---

## Section to write: {{ section.title }}

{{ section.guidance }}

Pay special attention to:
{{ section.emphasis }}

(The full chart above is provided so you can also draw on anything else genuinely relevant to this section — the focus above is guidance, not a limit.)

Timeline: this is a natal chart with no luck-period data, so anchor any timing to the pillar a signal sits on — the year pillar ≈ childhood and family origins; the month pillar ≈ youth and the career-building years (roughly the 20s–40s); the day pillar ≈ midlife; the hour pillar ≈ later life. A void, clash, harm or break on a pillar concentrates turbulence in that phase (e.g. a void month pillar ⇒ an unstable, setback-prone early career that steadies later). Speak in life phases — never invent specific ages or calendar dates.

{% if section.categories %}
Organise this section into these groups, in order:
{% for cat in section.categories %}- {{ cat.key }} — {{ cat.label }}
{% endfor %}
Use these EXACT JSON keys for the groups: {{ section.categories | map(attribute='key') | join(', ') }}. Do not translate, pluralize, or rename them — the headings to the right of each key are display labels only, never JSON keys.

Each group is a list of items, and each item has a "point" (one crisp claim) and an "explanation" (two to four sentences grounding that claim in this chart's specific evidence, per the guidance above). Each point must make a DISTINCT CLAIM — a fact a reader would recognise as different from the others, not the same conclusion restated from a different star. When several signals point to the SAME conclusion (e.g. a void pillar, 驿马 and the mourning stars all saying "siblings scattered and distant"), CONVERGE them into one richer point that cites them together — never split one conclusion across two near-duplicate points to lengthen the list. Within that constraint, include every genuinely distinct claim the chart supports: a rich domain may yield four to six, so before moving on, scan the whole chart again for further distinct claims you have not yet made — do not lazily stop at three when more genuinely exist. But a domain the chart speaks little to — siblings is often one — may honestly support only two or three distinct claims, and stopping there is correct. Never pad with repetition, a restated conclusion, or generic filler to reach a number.

Do NOT re-explain a signal across groups. Once you have explained a stem, branch, star, vault or interaction in one group, a later group may reference its conclusion in a single clause (e.g. "because that same money-star sits empty…") but must NOT re-derive it from scratch — spend that group's space on signals not yet used. Each group should advance the picture with fresh evidence, not restate earlier groups under a new heading.

Stay internally consistent. A later point must not assert something an earlier point — in the same group or another — has already made unlikely: if you have read a relative, theme or resource as absent, weak, scattered or void, do not then claim a strong, present instance of it. When a classical reading offers alternatives (e.g. a star whose text says "a parent OR a sibling of standing"), do NOT silently force one branch to fill a group — choose the branch that fits the rest of the chart (if the sibling palace is void or the person reads as an only child, attribute it to the parent), or keep the alternation explicit. Where two signals genuinely pull opposite ways, name the tension and give the better-supported reading rather than stating both as facts.

Return ONLY this JSON object, nothing else:
{"{{ section.key }}": {{ '{' }}{% for cat in section.categories %}"{{ cat.key }}": [{"point": "...", "explanation": "..."}]{% if not loop.last %}, {% endif %}{% endfor %}{{ '}' }}}
{% else %}
Write the "{{ section.title }}" section as flowing, connected paragraphs (the person's story for this part of their life). No bullet points, headings, or labelled fields.

Return ONLY this JSON object, nothing else:
{"{{ section.key }}": "...your paragraphs here..."}
{% endif %}
